{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "^14.4.2",
    "@sap/html5-repo-mock": "^2.1.5"
  },
  "scripts": {
    "build-asap_app": "cd asap_app && if [ ! -d ${workspaceFolder}/asap_app/asap_app/node_modules/react-scripts ] && [ ! -d ${workspaceFolder}/asap_app/node_modules/react-scripts ]; then npm i; fi && npm run build"
  }
}
